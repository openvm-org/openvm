[workspace]
members = [
    "calculator",
    "string-utils"
]

[workspace.package]
version = "0.0.0"
edition = "2021"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(openvm_intrinsics)'] }
