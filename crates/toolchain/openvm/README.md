# OpenVM Standard Library

The OpenVM standard library for use in Rust guest programs.

The libc memory intrinsics (`memcpy`, `memset`, ...) that guest code links against live in the
[`openvm-mem`](../mem) crate.
