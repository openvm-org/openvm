[package]
name = "u256-example"
version = "0.0.0"
edition = "2021"

[workspace]
members = []

[dependencies]
openvm = { git = "https://github.com/openvm-org/openvm.git", branch = "chore/ecc-cleanup", features = [
    "std",
] }
openvm-ruint = { git = "https://github.com/openvm-org/openvm.git", package = "ruint", branch = "chore/ecc-cleanup" }

[features]
default = []
