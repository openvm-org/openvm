[package]
name = "openvm-ecc-guest"
description = "OpenVM guest library for elliptic curve cryptography"
version.workspace = true
authors.workspace = true
edition.workspace = true
homepage.workspace = true
repository.workspace = true

[dependencies]
openvm = { workspace = true }
openvm-weierstrass-guest = { workspace = true }
openvm-edwards-guest = { workspace = true }

[features]
default = []
ed25519 = ["openvm-edwards-guest/ed25519"]
halo2curves = ["openvm-weierstrass-guest/halo2curves", "openvm-weierstrass-guest/halo2curves"]
std = ["openvm-weierstrass-guest/std", "openvm-weierstrass-guest/std"]
alloc = ["openvm-weierstrass-guest/alloc", "openvm-weierstrass-guest/alloc"]
