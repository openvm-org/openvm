// The legacy launcher remains available under `rvr`; direct CALL replay is
// intentionally not exposed until canonical chronology can represent AS4
// field-cell events without truncation.
#include "../src/call.cu"
