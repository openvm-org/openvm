[workspace]
[package]
name = "axvm-ecdsa-recover-key-program"
version = "0.0.0"
edition = "2021"

[dependencies]
axvm = { path = "../../../toolchain/riscv/axvm" }
# TODO: don't use local when changes merged
revm-precompile = { path = "../../../../revm/crates/precompile", default-features = false, features = ["axvm"] }
revm-primitives = { path = "../../../../revm/crates/primitives", default-features = false }
k256 = { version = "0.13.3", default-features = false }
hex-literal = { version = "0.4.1", default-features = false }

[features]
default = []
std = ["axvm/std"]
