[workspace]
[package]
name = "test_kernel"
version = "0.0.0"
edition = "2021"

[dependencies]
openvm = { path = "../../../crates/toolchain/openvm" }
openvm-native-guest-macro = { path = "../../../extensions/native/guest-macro" }

[features]
default = []
std = ["openvm/std"]
