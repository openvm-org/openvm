[workspace]
[package]
name = "axvm-test-programs"
version = "0.0.0"
edition = "2021"

[dependencies]
axvm = { path = "../../riscv/axvm" }
axvm-bigint-guest = { path = "../../../extensions/bigint/guest" }
axvm-keccak256-guest = { path = "../../../extensions/keccak256/guest" }
axvm-platform = { path = "../../riscv/platform" }
axvm-ecc = { path = "../../../lib/ecc", default-features = false }
axvm-algebra = { path = "../../../lib/algebra" }
serde = { version = "1.0", default-features = false, features = [
    "alloc",
    "derive",
] }
hex = { version = "0.4.3", default-features = false, features = ["alloc"] }
hex-literal = { version = "0.4.1", default-features = false }
k256 = { version = "0.13.3", default-features = false, features = [
    "ecdsa-core",
    "ecdsa",
], optional = true }

[features]
default = []
std = ["axvm/std", "axvm-ecc/std", "axvm-keccak256-guest/std"]
bn254 = ["axvm-ecc/bn254"]
bls12_381 = ["axvm-ecc/bls12_381"]
k256 = ["axvm-ecc/k256", "dep:k256"]

[profile.release]
panic = "unwind"
lto = "fat"      # decrease binary size
# strip = "symbols"

[[example]]
name = "ec"
required-features = ["k256"]

[[example]]
name = "ecdsa"
required-features = ["k256"]

[[example]]
name = "final_exp_hint"
required-features = ["bls12_381"]
